// DB 스키마 생성하는 use case + Type 업데이트 해줘야 함
"use server";

export default function generateSchemaCodeUseCase() {
    // use_case title/description 정보 가져와서 storage,code에 저장
    // type 업데이트하기
    // llm_response에 저장
}

// 태은