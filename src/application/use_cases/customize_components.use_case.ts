// Shadcn UI 컴포넌트 커스터마이즈하는 use case
"use server";

export default function customizeComponentsUseCase() {
    // 색상 조합, 라운드, 그림자, 폰트 등 -> 유저가 선택 -> node.type === style에 저장
    // code tailwind.config.ts 코드 수정
}
