// 프로젝트 생성하는 use case
"use server";

export default async function createEmptyProjectUseCase() {
    // vercel api, supabase api + url, project.title 받음
    // project row 생성
    // 기본 코드 템플릿을 supabase storage에 저장
    // 기본 코드 템플릿으로 vercel 배포
}
